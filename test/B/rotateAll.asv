% Script to rotate images of folder B and save them 

for i = 1:1:41
count = 1;
filename = ['B-complex' num2str(count) '.ppm'];

image = imread(filename);

image = rot90(rot90(rot90(image)));

imwrite(image,filename);